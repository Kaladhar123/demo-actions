name: upper Build

on:
  push:
    paths: 
      - ".github/workflows/upper-buils.yml"
  #workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - run:  go version
      - run: pwd && ls -ltrh
